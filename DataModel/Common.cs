using System;
using System.Collections.Generic;
using System.Text;

namespace DataModel
{
    // This is where I put the thing I use in several different classes, so I don't have to create a new
    // object in every class. 
    class Common
    {
        public static Random random = new Random();
    }
}
